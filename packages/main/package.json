{
  "name": "main",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "build": "electron-builder build --linux dir --mac dir",
    "dev": "DISPLAY=:1 NODE_ENV=development electronmon . --no-sandbox",
    "test": "tsc --noEmit"
  },
  "dependencies": {
    "renderer": "workspace:*"
  },
  "devDependencies": {
    "electron": "37.4.0",
    "electron-builder": "26.0.20",
    "electronmon": "2.0.3"
  }
}
